# Polymarket Trading Bot Requirements

# Core dependencies
requests>=2.31.0

# Polymarket official client (required for trading)
py-clob-client>=0.17.0

# Polymarket community APIs (for redeem functionality)
# Requires Python 3.12+
polymarket_apis>=0.4.0

# Web3 dependencies (required by py-clob-client)
web3>=6.0.0
eth-account>=0.9.0

# Async HTTP with HTTP/2 support (for optimized version)
httpx[http2]>=0.25.0

# Utilities
python-dotenv>=1.0.0  # For environment variables
python-dateutil>=2.8.2  # For date parsing
pytz>=2023.3  # For timezone handling

# WebSocket for real-time price updates
websockets>=12.0

# Fast JSON parsing (5-10x faster than stdlib json)
orjson>=3.9.0

